


// 先实现旋转。

const mousedown = e => {
    // if ()

    // 旋转。
    
    // 检查按的是哪个点。
}

const mouseup = e => {

}



// const selectorAction =  {

// }

// transformBox
/* function tbox(el) {
    const bbox = el.bbox();
    const m = el.transform();


    tPoint()
    
    return coords;
}

function tPoint(x, y, m) {
    return {x: m.a * x + m.c * y + m.e, y: m.b * x + m.d * y + m.f};
} */