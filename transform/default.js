

export const defaultTransform = `rotate(0, 0, 0) translate(0, 0) scale(1, 1) translate(0, 0)`



const translateUtil = {
    // 解析字符串
    resolve(str) {
        // const pattern = /rotate\((\d+(?:\.\d+)?)\)/
        
    },

    multiply() {

    },

}

